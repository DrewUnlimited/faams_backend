# FAAMS Backend


## 🔧 Setup Instructions

### 1. **Install Backend Dependencies**

Navigate to your project directory and install the backend requirements with `pip`:

```bash
pip install -r requirements.txt
```

### 2. **Run Fast API**

Run Fast API Uvicorn Server by running the following command

```bash
uvicorn main:app --reload
```
